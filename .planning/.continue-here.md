---
context: default
phase: project-kings-mvp-live-handoff
task: 2
total_tasks: 5
status: paused
last_updated: 2026-07-12T13:53:57+0300
---

# Project Kings MVP — точный handoff

## Current state

Цель не изменилась: Dark JoyBoy, THE LIGHT KINGDOM и COP SCOPES должны
получить по три **новых** Shorts; каждый из девяти должен стать
`public_verified` на правильном YouTube-канале. Затем для каждого канала нужен
next-ready, daily daemon + refiller должны быть armed/running, а первый
естественный scheduled wake — войти в durable processing. До этого карточка
Workspace не закрывается.

Runtime code commit: `6d93bda5bbbb9e2e0bf9c1aeeb34bb3f1be15598`
(`6d93bda`) in `wip/project-kings-pipeline-v1-20260710`; it is pushed.
The current branch tip also contains this handoff documentation commit.
Локальный Clips worktree чист, кроме user-owned untracked `experiments/`.

Последняя фактическая проверка production buffer 12 июля:

| Profile | Qualified | State |
|---|---:|---|
| Dark JoyBoy | 12 | выше minimum, не требует refill |
| THE LIGHT KINGDOM | 2 | блокирует shadow/live |
| COP SCOPES | 1 | блокирует shadow/live |

`ready=false`. Никаких новых YouTube публикаций или upload/public identities
не создано. Неудачный shadow run `55186e0f0f9c48a7968cae94fe1e8d37` завершился
`blocked` и является только audit evidence.

## BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: ASR must stay fail-closed — отсутствие Whisper `.txt` допустимо
  только когда media inspection доказал отсутствие аудиодорожки; при аудио это
  processing failure, а не «тишина». Это предотвращает passage речи мимо
  source_policy/source_fit.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Treating missing ASR as empty speech | Первый refill получил `ENOENT` after Whisper and тем самым потерял кандидата; широкий catch мог бы скрыть speech. | blocking | Код `cf89b8d` запускает Whisper только для media with audio; missing output with audio throws. Keep tests green. |
| Calling a failed shadow a release | Earlier 1x3 had no publications and stopped on source/revision failures. | blocking | Require new buffer `>=6` each, fresh shadow, then exact 9 `public_verified`; inspect external IDs independently. |
| Treating the bootstrap catalog as infinite automation | Catalog-only path intentionally replaces dynamic discovery and ends when its finite entries end. | advisory | Use only to resolve the current deficit; document its SHA and remaining entries, then choose an ordinary discovery/reserve renewal path before it exhausts. |

## Completed work

- `6066fd6`: recovery/refill durability fixes.
- `9c2c246` + `cf89b8d`: silent-media handling is safe: no-audio is explicit;
  audio with missing Whisper output fails closed.
- `6d93bda`: bounded frozen reserve catalog. It requires a mode-`0600` JSON,
  validates canonical URL/provider/route/donor against frozen policy, binds its
  SHA into the refill request identity, and still runs every normal quality
  gate. The catalog is only selected when
  `PROJECT_KINGS_SOURCE_REFILL_CATALOG_PATH` is set.
- Validation on `6d93bda`: 17 autonomous-refill tests PASS, 19 CLI/launchd
  tests PASS, TypeScript typecheck PASS, targeted ESLint/diff check PASS, and
  production Next build PASS (only existing `<img>` warnings).
- Zoro isolated runtime is detached at `6d93bda` and import-smoke passed.
  Both portfolio and refiller LaunchAgents are unloaded/disarmed.

## Current external infrastructure

- Render service `srv-d6lde1h5pdvs73f6vqq0` is still live on `008216e`, not on
  `6d93bda`. AutoDeploy is off. The authenticated dashboard can manually deploy
  an exact commit. `PORTFOLIO_PIPELINE_V1_ENABLED=0` and
  `PORTFOLIO_PIPELINE_POST_CANARY_ENABLED=0` must remain off until the
  controlled live stage.
- Zoro runtime:
  `/Users/neichyabazhi/.local/share/clips-project-kings-runtime-5c1bfb3`, HEAD
  `6d93bda`. Semantic worker is online; ordinary renderer must not be touched.
  `launchctl print` exit `113` for both portfolio/refiller confirms they are
  unloaded.
- Continuous authorization:
  `/Users/neich/Documents/Macedonian Imperium/Assistant/.assistant/task-packets/project-kings-mvp/owner-continuous-execution-authorization-2026-07-11.json`, SHA
  `9b8e527607aaa6360c7d1780da47f875bd83df5188de2ae2b80a3d05164701d8`.
  It authorizes the remaining bounded execution; do not ask again.

## Required reading, in order

1. `.planning/HANDOFF.json` — machine-readable task state.
2. `docs/project-kings-production-pipeline-v1/LOOP-SPEC.md` — release outcome
   and safety contract.
3. `docs/project-kings-production-pipeline-v1/source-refill-contour.md` —
   catalog/bootstrap and refill semantics.
4. `/Users/neich/Documents/Macedonian Imperium/Assistant/.assistant/task-packets/project-kings-mvp/HANDOFF-2026-07-12.md` — cross-system, owner-facing handoff.
5. `experiments/source-researcher/runs/ask/2026-07-12-light-mvp-refill/` —
   45 raw Ask candidates; 20 exist/title/duration-verified. No frame judging or
   upload was performed in this run.
6. `experiments/source-researcher/runs/2026-07-12-cop-mvp-refill/verdicts.json`
   — current Cop screening evidence; preserve it.

## Exact next execution sequence

1. Build a private catalog file on Zoro from the stored research, with up to
   nine **canonical** candidates per deficient profile. Light entries must be
   `youtube_ask` / `youtube_ask_v3` / `donorUsername:null`; Cop entries must be
   `instagram` / `instagram_donor_pool` / `donorUsername:"copscopes"`. Hash it
   using `hashProjectKingsFrozenDiscoveryCatalog`, set mode `0600`, and set the
   catalog env path only for this bootstrap run.
2. Run the existing autonomous refiller in bounded `execute` with its current
   machine credential; it is already authorized. Do not bypass policy/source-fit
   or upload directly. Read ledger and production source-buffer endpoint after
   each run. If at least one source is admitted, a new runtime snapshot permits
   the next bounded request; keep exact rejected evidence.
3. Independently verify Dark/Light/Cop are each `>=6` (prefer 12 for Light/Cop)
   and `ready=true` before fresh shadow.
4. In the authenticated Render dashboard, deploy **exact commit `6d93bda`** and
   verify service becomes Live at that commit. Do not enable the two portfolio
   flags.
5. Promote the three approved exact profile versions, validate, then run a
   fresh non-public shadow `1x3`. Acceptance: three final outputs, independent
   QA, no publication/upload identity.
6. Only after shadow acceptance run the authorized controlled live `1x3`, wait
   for all three unique `public_verified`, then release `2x3`. Stop on any
   unknown upload/destination/duplicate condition.
7. After 9/9, refill to next-ready, arm/load daemon and refiller, and observe
   one natural scheduled wake. Only then close Workspace task.

## Workspace / task routing

- Workspace task: `045f1394-3db6-470a-ae7a-3c1b5bae59d4`.
- Workspace project: `ЗАПУСК 30 КАНАЛОВ` / `f63a9467-c747-4e27-a579-84b6f6e1c00d`.
- Existing continuation thread: `019f524f-a6f8-73e0-aeed-d6aaf5f9e2ad`.

Start with step 1 only. Do not re-audit historical code or rebuild a general
source-discovery platform before the current finite deficit is resolved.
